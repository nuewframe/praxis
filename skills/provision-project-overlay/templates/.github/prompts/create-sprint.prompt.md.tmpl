---
mode: agent
description: Create a new sprint document for {{project.name}}.
---

Run the project's `create-sprint` skill at `.github/skills/create-sprint/SKILL.md`, which is a thin overlay over the `praxis` plugin's `create-sprint` skill.

Default sprint file: `{{paths.sprints}}/SPRINT.<ID>-<slug>.md`.
