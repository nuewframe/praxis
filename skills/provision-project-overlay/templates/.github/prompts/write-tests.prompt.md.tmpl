---
mode: agent
description: Write tests for {{project.name}} following the Pyramid Test Strategy.
---

Run the project's `write-tests` skill at `.github/skills/write-tests/SKILL.md`, which is a thin overlay over the `praxis` plugin's `test-by-ownership` skill.

Quality gate: `{{quality_gates.test}}` must pass before the work is considered complete.
