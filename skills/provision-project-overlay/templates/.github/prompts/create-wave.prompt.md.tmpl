---
mode: agent
description: Create a new product wave for {{project.name}}.
---

Run the project's `create-wave` skill at `.github/skills/create-wave/SKILL.md`, which is a thin overlay over the `praxis` plugin's `create-wave` skill. The overlay supplies project paths and gates; the plugin supplies the four-document pattern and content discipline.

Default wave directory: `{{paths.waves}}/wave-NNN-<slug>/`.
