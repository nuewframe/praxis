# {{personas.aliases.principal-engineer}} — Persona Pointer

The {{personas.aliases.principal-engineer}} persona for {{project.name}} is defined by:

- **Plugin agent:** `principal-engineer` from the `praxis` plugin (universal engineering discipline)
- **Project alias:** `.github/agents/{{personas.aliases.principal-engineer}}.agent.md` ({{project.name}} overlay with stack and paths)
- **Always-on guardrails:** `.github/instructions/{{personas.aliases.principal-engineer}}-engineer.instructions.md` (file shape, error handling, logging, naming for {{stack.runtime}} + {{stack.api_framework}} + {{stack.database}})
- **Architecture overlay:** `.github/instructions/{{personas.aliases.principal-engineer}}-architecture.instructions.md` (when authoring `{{paths.waves}}/**/product-architecture.md`)

There is no separate full-persona document. The plugin agent + project alias cover everything previously in this file.
