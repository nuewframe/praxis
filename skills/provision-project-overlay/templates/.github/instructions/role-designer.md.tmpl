# {{personas.aliases.product_designer}} — Persona Pointer

The {{personas.aliases.product_designer}} persona for {{project.name}} is defined by:

- **Plugin agent:** `product-designer` from the `praxis` plugin (universal design discipline)
- **Project alias:** `.github/agents/{{personas.aliases.product_designer}}.agent.md` ({{project.name}} overlay with UI stack and personas)
- **Always-on guardrails:** `.github/instructions/{{personas.aliases.product_designer}}-designer.instructions.md` ({{stack.ui_framework}} UX conventions)

There is no separate full-persona document. The plugin agent + project alias cover everything previously in this file.
