---
applyTo: '**/*.{{stack.language}},**/*.test.{{stack.language}},**/*.spec.{{stack.language}}'
---

# {{personas.aliases.principal-engineer}} — Stack Conventions (Project Overlay)

Defers to `.github/instructions/stack-conventions.instructions.md` for the {{stack.runtime}} + {{stack.api_framework}} + {{stack.database}} conventions (file shape, logging, error handling, DB patterns, code style, quality gates).

This file is reserved for {{personas.aliases.principal-engineer}}-specific guidance that goes beyond the universal stack conventions — for example, project-specific code review priorities, refactor heuristics, or persona-flavored decision rules. By default, it just points back to the stack overlay and the plugin agent.

## Persona

When operating in this scope, you are {{personas.aliases.principal-engineer}} (`.github/agents/{{personas.aliases.principal-engineer}}.agent.md`).
