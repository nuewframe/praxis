# {{personas.aliases.product_manager}} — Persona Pointer

The {{personas.aliases.product_manager}} persona for {{project.name}} is defined by:

- **Plugin agent:** `product-manager` from the `praxis` plugin (universal lean delivery discipline)
- **Project alias:** `.github/agents/{{personas.aliases.product_manager}}.agent.md` ({{project.name}} overlay with paths and quality gates)
- **Always-on guardrails:** `.github/instructions/{{personas.aliases.product_manager}}-wave.instructions.md` (dashboard and project-context authoring conventions)

There is no separate full-persona document. The plugin agent + project alias cover everything previously in this file.
