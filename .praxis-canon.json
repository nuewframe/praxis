{
  "_comment": "Canonical-facts registry for Praxis. Machine-checkable single-source-of-truth guards consumed by validate-plugin.sh (Phase 1). Extend forbiddenTerms as terminology evolves.",
  "forbiddenTerms": [
    {
      "pattern": "\\bthe bet\\b",
      "reason": "wave product-architecture is 'the educated theory', not 'the bet'"
    },
    {
      "pattern": "validated bet",
      "reason": "use 'validated educated theory'"
    },
    {
      "pattern": "= bet;",
      "reason": "mantra is 'Wave = educated theory; capability record = truth'"
    },
    {
      "pattern": "three-axis disjointness",
      "reason": "renamed to 'four-condition disjointness' (the rule has four conditions)"
    },
    {
      "pattern": "principal-engineer plugin",
      "reason": "the plugin was renamed to 'praxis'"
    },
    {
      "pattern": "Logic \u2192 Component \u2192",
      "reason": "canonical test pyramid is Logic -> Composition -> Adapter Contract -> Integration -> Journey"
    },
    {
      "pattern": "profile:\\s*lite",
      "reason": "the lite/standard adoption profile dial was cut pre-release (never shipped in a tagged version, confirmed via git merge-base against v0.3.0) -- full is the only supported profile; do not reintroduce without real-repo evidence per the evolution policy"
    },
    {
      "pattern": "lite \\| standard \\| full",
      "reason": "the three-way adoption-profile enum was cut pre-release; full is the only supported profile"
    },
    {
      "pattern": "by design, not by oversight",
      "reason": "meta-commentary anti-pattern: defensive throat-clearing that pre-empts an unmade challenge. State the reason as a bare flat assertion instead."
    }
  ],
  "terminologyScanDirs": [
    "skills",
    "instructions",
    "agents",
    "docs"
  ],
  "placeholderConfigTemplate": "skills/provision-project-overlay/templates/praxis.config.yaml.tmpl",
  "placeholderScanGlob": "skills/provision-project-overlay/templates",
  "specialPlaceholders": [
    "TODAY"
  ],
  "requiredPhrases": [
    {
      "file": "README.md",
      "pattern": "are \\*\\*not\\*\\* mechanically compelled",
      "reason": "README's opening pitch must disclose that not every gate is equally binding, so an adopter does not over-trust a green check. The pattern is the honesty CLAIM rather than the list \u2014 the list was three kinds, is four since the delivery graph, and encoding a count in a check makes the check stale the next time the truth improves."
    },
    {
      "file": "skills/provision-project-overlay/SKILL.md",
      "pattern": "this repository is one \\*\\*part\\*\\*",
      "reason": "ADR.260725.17: when paths.product_root is set the repository is a PART of a larger product, and its dashboard must declare that scope rather than presenting a fragment as the whole. This binds the overlay skill to state the rule it must apply. The failure mode is not a missing dashboard but a confident one, so the phrase is what the check can hold."
    }
  ]
}
