// Opened by `praxis pick-up`. Every condition below was evaluated against the
// tree as it stood at 2026-08-21T19:42:09Z, and the verdicts are recorded
// rather than summarised: a gate whose reasoning is not on the record is a gate
// you have to trust.

iteration "ITER.260821.13" {
    slug "review-an-iteration-in-flight"
    on-slice "TS.260820.08"
    state "closed"
    opened-at "2026-08-21T19:42:09Z"
    opened-by "agent:praxis"

    approval "admission" {
        status "signed"
        signer "human:wael"
        at "2026-08-21T19:42:09Z"
        signed-by "the ask to pick up this slice"
    }

    vet "create" state="done" at="2026-08-21T19:42:09Z" {
        condition "slice-is-shaped" verdict="passed"
        condition "dependencies-delivered" verdict="passed"
        condition "no-iteration-in-flight" verdict="passed"
        condition "nothing-left-to-admit" verdict="passed"
        condition "work-is-disjoint" verdict="not-computed" \
            found="no seam manifest exists, so what two iterations would touch cannot be compared. Disjointness is half of what this capability says it judges, and half of what it judges is currently not computed at all"
        condition "the-slice-is-the-best-available" verdict="left-to-the-maintainer"
    }

    // 2 of the declared conditions were not decided by the gate. They are
    // recorded above rather than omitted: an admission that does not say what
    // it left undecided claims more than it checked.

    // Frozen at open, from the slice. Removing one from the slice now
    // does not remove it from here — it makes the two disagree, and that is
    // refused.

    claim "C1" from-slice="TS.260820.08" state="met" \
        text="a preview is never mistakable for a record — the header says which it is" \
        settled-by="a golden test on the preview header" \
        evidence="a preview declares itself a preview asserts it in the \
            RESULT, and reviewing leaves the published tree byte-unchanged \
            asserts the rendered header. Both, because a marker only the renderer applies is one a \
            second renderer can forget"
    claim "C2" from-slice="TS.260820.08" state="met" \
        text="composing a preview writes nothing to the published tree" \
        settled-by="a test asserting the published tree is unchanged after a preview" \
        evidence="reviewing leaves the published tree byte-unchanged — a \
            real repository with a real published document, byte-compared before and after" \
        note="the stronger half is structural: `review` returns a ReadModel and nothing else, so a \
            preview has no path in its type and nowhere in the published tree to land"
    claim "C3" from-slice="TS.260820.08" state="met" \
        text="the preview reflects state at the moment it was asked, with no regeneration step" \
        settled-by="a test that mutates state between two previews and asserts they differ" \
        evidence="a preview reflects the moment it was asked"

    claim "C2" from-slice="TS.260820.06" state="met" \
        text="a declared layer with no evidence is visible as unevidenced, not absent" \
        settled-by="the review view listing the layer with no artifact rather than omitting it" \
        evidence="a declared layer with no evidence is listed not omitted — \
            layers come from the SLICE, so one the iteration never touched appears as unevidenced \
            rather than vanishing from the table" \
        note="the residue U3, carried from ITER.260821.05. It was settled there against the claim's \
            TEXT, by a checker report; this settles the mechanism the claim actually named"

    vet "start" state="done" at="2026-08-21T19:40:00Z" {
        condition "the tree is clean and current" verdict="passed" found="HEAD 1d42d7d"
        condition "the slice's scope is unchanged" verdict="passed" found="three claims, read line by line"
        condition "U3 can be settled here" verdict="passed" \
            found="this is the slice U3 named. The review view is where a declared-but-unreached \
                layer has to be visible, and layers are read from the slice rather than the \
                iteration, which is what makes the omission impossible"
    }

    frozen {
        pins "the entity-and-field schema" at="NA.260820.01/schema" at-commit="1d42d7d"
        pins "read-model@v1" at="NA.260820.01/seam" at-commit="1d42d7d"
        rule "a schema change while this iteration is open is a FINDING, not an edit" \
            exception="none. This iteration changed no schema"
    }

    contributes "slice-outcome-added" \
        because="review-an-iteration-in-flight, and the gap between promised and shown in one answer"

    layer "doctrine"    state="evidenced" evidence="surface.skill.review-in-flight, registered in the router" reaches="the skill teaches reviewing in flight rather than at a checkpoint"
    layer "enforcement" state="evidenced" evidence="124 tests pass; the working projection is gitignored, and the composing function has no path in its type" reaches="the working projection path is gitignored, so a preview cannot be committed by accident"
    layer "docs"        state="evidenced" evidence="the Markdown preview carries `PREVIEW — not a record` above anything a reader might quote" reaches="the preview itself, marked and discarded"
    layer-set sealed=#true at="2026-08-21T19:40:00Z"

    phase "design-ux" state="complete" required-by="docs" \
        produced="the marker goes ABOVE anything a reader might quote, not in a footer — a preview \
            pasted into a message loses its footer and keeps its header. And `what it promised` \
            carries a `what has been shown` column rather than only a state, because `pending` and \
            `carried by F1` are different answers that a state alone flattens"
    phase "design-system" state="complete" required-by="enforcement" \
        produced="layers are read from the SLICE and matched against the iteration, not read from \
            the iteration. That single choice is what makes an unreached layer impossible to omit — \
            the table is built from what was declared, and the iteration only fills it in"
    phase "implement" state="complete" required-by="enforcement · docs" \
        produced="praxis-core::review, `praxis review --markdown`, the preview header in the \
            Markdown renderer, the gitignore entry, and nine tests"
    phase "learn" state="complete" required-by="always" produced="three findings" \
        note="never skippable — it is the phase that judges the others"
    phase "teach" state="skipped" required-by="doctrine" produced="surface.skill.review-in-flight" \
        skipped-because="it produced doctrine for an AGENT, which is the doctrine LAYER and is recorded there. Teaching an end user the feature's why and how did not happen — and the record already serves the next agent, whose own persona judges success by NOT needing a document (TS.260821.19, ITER.260822.15/BA1)"

    finding "AC1" state="open" size="answered" \
        text="U3 is settled at the mechanism it named. ITER.260821.05 settled TS.260820.06/C2 \
            against the claim's TEXT — the layer was visible because the checker reported it — and \
            recorded that the review view still owed the mechanism. It is delivered here" \
        note="the first residue in this frame settled against the exact route its claim named rather \
            than against a substitute. Worth contrasting with X1 and S1, both of which were settled \
            by construction: this one was settled by building the thing"

    finding "AC2" state="open" size="observation" \
        text="the second E10 test passes. Content owned by claim-settlement, mechanism owned by \
            document-projection, and rendering this view needed no code path specific to it — the \
            only renderer change was a PREVIEW header, which keys on `publishable` and knows nothing \
            about which view it is looking at" \
        note="that is three views and two renderers now, with one conditional between them, and the \
            conditional is on a property of the RESULT rather than on the view's identity. The \
            falsifier has had every chance this frame can give it and has not fired"

    finding "AC3" state="open" size="observation" \
        text="this iteration previewed ITSELF while in flight — `praxis review ITER.260821.13` — and \
            correctly showed three unevidenced layers and three unmet claims" \
        note="the first time a tool in this frame was used on the work that was building it, at the \
            moment it was being built. Every prior command was exercised on records that were \
            already finished, which is a weaker test: a finished record is one where the answer is \
            already known"
    closed-at "2026-08-21T19:45:41Z"
    outcome "continue"

    close {
        claims-settled "C1" how="met"
        claims-settled "C2" how="met"
        claims-settled "C3" how="met"
        claims-settled "C2" how="met"
        all-met #true
        note "the accounting above was computed at close, not asserted. It says every claim is either met or carried by a finding that names it, and nothing about whether the work is any good"
    }

    trail {
        entry at="2026-08-21T19:45:41Z" by="agent:praxis" action="state-changed" from="open" to="closed" note="closed by `praxis close`. The accounting was computed; the conclusion is not the machine's to draw"

        entry at="2026-08-21T19:42:09Z" by="agent:praxis" action="created" \
            note="opened by the gate, on the ask of human:wael"
    }
}
