// Opened by `praxis pick-up`. Every condition below was evaluated against the
// tree as it stood at 2026-08-21T19:48:09Z, and the verdicts are recorded
// rather than summarised: a gate whose reasoning is not on the record is a gate
// you have to trust.

iteration "ITER.260821.14" {
    slug "declare-what-gets-published"
    on-slice "TS.260820.12"
    state "closed"
    opened-at "2026-08-21T19:48:09Z"
    opened-by "agent:praxis"

    approval "admission" {
        status "signed"
        signer "human:wael"
        at "2026-08-21T19:48:09Z"
        signed-by "the ask to pick up this slice"
    }

    vet "create" state="done" at="2026-08-21T19:48:09Z" {
        condition "slice-is-shaped" verdict="passed"
        condition "dependencies-delivered" verdict="passed"
        condition "no-iteration-in-flight" verdict="passed"
        condition "nothing-left-to-admit" verdict="passed"
        condition "work-is-disjoint" verdict="not-computed" \
            found="no seam manifest exists, so what two iterations would touch cannot be compared. Disjointness is half of what this capability says it judges, and half of what it judges is currently not computed at all"
        condition "the-slice-is-the-best-available" verdict="left-to-the-maintainer"
    }

    // 2 of the declared conditions were not decided by the gate. They are
    // recorded above rather than omitted: an admission that does not say what
    // it left undecided claims more than it checked.

    // Frozen at open, from the slice. Removing one from the slice now
    // does not remove it from here — it makes the two disagree, and that is
    // refused.

    claim "C1" from-slice="TS.260820.12" state="met" \
        text="a read model with no declared lifetime is refused" \
        settled-by="a negative test asserting the read-model name in the diagnostic" \
        evidence="tests/lifetimes.rs::c1_a_read_model_with_no_declared_lifetime_is_refused — the \
            refusal names the view and says there is no repository-wide default, because the reader \
            who hits it will want to know whether one exists"
    claim "C2" from-slice="TS.260820.12" state="met" \
        text="a publishable declaration with no reason is refused" \
        settled-by="a negative test with publishable set and no because" \
        evidence="tests/lifetimes.rs::c2_a_publishable_declaration_with_no_reason_is_refused, and \
            ::an_on_demand_declaration_needs_a_reason_too — the rule runs in BOTH directions, \
            because deciding not to publish is as much a judgement about perishability as deciding to"
    claim "C3" from-slice="TS.260820.12" state="met" \
        text="the published set equals exactly the publishable declarations — a hand-listed set is refused" \
        settled-by="a test asserting the generated set matches the declarations, plus a negative test on a hand-edited list" \
        evidence="tests/lifetimes.rs::c3_the_published_set_equals_exactly_the_publishable_declarations, \
            ::c3_the_paths_come_from_the_record_not_from_the_engine, and \
            ::c3_a_declared_view_the_engine_cannot_compose_refuses_the_whole_publish — a set that \
            quietly omits a declared member is a hand-maintained list with extra steps"

    vet "start" state="done" at="2026-08-21T19:47:00Z" {
        condition "the tree is clean and current" verdict="passed" found="HEAD beaed9d"
        condition "the slice's scope is unchanged" verdict="passed" found="three claims, read line by line"
        condition "this will break publishing on the real tree" verdict="known-and-recorded" \
            found="the storm declares four publishable views and the engine has composers for two. \
                Deriving the set means `praxis publish 0.8.0` refuses until TS.260820.14 and \
                TS.260820.15 supply the other two" \
            resolved-by="accepted. A refusal is the correct answer — the set must EQUAL the \
                declarations — and the two slices that clear it are next. Recorded as AD2"
    }

    frozen {
        pins "the entity-and-field schema" at="NA.260820.01/schema" at-commit="beaed9d"
        pins "the lifetime declarations" at="ES.260819.01 — read-model" at-commit="beaed9d"
        rule "a schema change while this iteration is open is a FINDING, not an edit" \
            exception="declaring publish-only-what-survives-freezing is the mechanism this slice \
                depends on"
    }

    contributes "slice-outcome-added" \
        because="declare-what-gets-published, and a published set derived from declared lifetimes"

    layer "doctrine"    state="evidenced" evidence="skills/declare-a-lifetime/SKILL.md, registered in the router" reaches="the skill teaches the publication test and its two outcomes"
    layer "enforcement" state="evidenced" evidence="133 tests pass; an undeclared lifetime, an unjustified one in either direction, and a publishable view with nowhere to land are each refused" reaches="publish-only-what-survives-freezing, refusing an undeclared or unjustified lifetime"
    layer "docs"        state="evidenced" evidence="`praxis publish` now composes its set from the declarations, at the paths the declarations name" reaches="the published set is derived from these declarations and from nothing else"
    layer-set sealed=#true at="2026-08-21T19:47:00Z"

    phase "design-ux" state="complete" required-by="docs" \
        produced="the refusal for an undeclared lifetime says there is NO repository-wide default, \
            because that is the first thing the reader will look for — and looking for it is how a \
            per-file-type default gets invented"
    phase "design-system" state="complete" required-by="enforcement" \
        produced="a declared view the engine cannot compose refuses the WHOLE publish rather than \
            publishing a shorter set. A set that quietly omits a member is a hand-maintained list \
            with extra steps, which is the thing being replaced — so the equality is enforced by \
            failing, not by warning"
    phase "implement" state="complete" required-by="enforcement · docs" \
        produced="the `View` corpus reader, publish-only-what-survives-freezing, membership derived \
            in praxis-core::publish, and nine tests"
    phase "learn" state="complete" required-by="always" produced="three findings, one of them about a decision the engine had quietly taken" \
        note="never skippable — it is the phase that judges the others"
    phase "teach" state="complete" required-by="doctrine" produced="skills/declare-a-lifetime/SKILL.md" \
        taught="the-next-agent" \
        taught="the-next-agent"

    finding "AD1" state="open" size="frame-sized" \
        text="the record had been carrying the right answer the whole time and nothing read it. \
            ES.260819.01 declares every read model's lifetime, its reason, and its `publishes-to` \
            path — and ITER.260821.09 built publish with a hardcoded list of two documents at paths \
            it made up. The declared paths are release-notes.md and capabilities/; the engine wrote \
            what-shipped.md and capabilities.md" \
        note="this is A4's failure mode arriving from the other side. A4 says the engine may not \
            hold a rule the record does not state; the subtler version is the engine holding a \
            DECISION the record already stated, where nothing is contradicted and nothing refuses. \
            The record and the engine simply disagreed, silently, and the engine won because it was \
            the one running"

    finding "AD2" state="open" size="owed-work" \
        text="deriving the set means `praxis publish 0.8.0` now refuses: four views are declared \
            publishable and the engine composes two. the-decisions-that-shaped-this and \
            how-to-use-a-capability are TS.260820.14 and TS.260820.15" \
        obliges="TS.260820.14 and TS.260820.15 — until both land, this release cannot publish" \
        note="the refusal is correct and the sequencing is now enforced rather than remembered. \
            Worth noticing that the two slices it blocks were already the next two: the record \
            derived an ordering that matched the one a human had chosen, which is the first time \
            those two have been compared"

    finding "AD3" state="open" size="observation" \
        text="the rule refuses an unjustified declaration in BOTH directions — `publishable=#false` \
            with no reason is refused exactly like `#true` with no reason" \
        note="not what the slice asked for. C2 names only the publishable case. But deciding not to \
            publish is the same judgement, and six months on nobody can tell `we thought about this` \
            from `nobody got to it`. Recorded because it is scope the slice did not ask for, and the \
            close should not be able to hide that"
    closed-at "2026-08-21T19:52:15Z"
    outcome "continue"

    close {
        claims-settled "C1" how="met"
        claims-settled "C2" how="met"
        claims-settled "C3" how="met"
        all-met #true
        note "the accounting above was computed at close, not asserted. It says every claim is either met or carried by a finding that names it, and nothing about whether the work is any good"
    }

    trail {
        entry at="2026-08-21T19:52:15Z" by="agent:praxis" action="state-changed" from="open" to="closed" note="closed by `praxis close`. The accounting was computed; the conclusion is not the machine's to draw"

        entry at="2026-08-21T19:48:09Z" by="agent:praxis" action="created" \
            note="opened by the gate, on the ask of human:wael"
    }
}
