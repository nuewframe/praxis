// Opened by `praxis pick-up`. Every condition below was evaluated against the
// tree as it stood at 2026-08-22T09:42:27Z, and the verdicts are recorded
// rather than summarised: a gate whose reasoning is not on the record is a gate
// you have to trust.

iteration "ITER.260822.08" {
    slug "ask-the-record"
    on-slice "TS.260821.11"
    state "closed"
    opened-at "2026-08-22T09:42:27Z"
    opened-by "agent:praxis"

    approval "admission" {
        status "signed"
        signer "human:wael"
        at "2026-08-22T09:42:27Z"
        signed-by "the ask to pick up this slice"
    }

    vet "create" state="done" at="2026-08-22T09:42:27Z" on-slice="TS.260821.11" {
        condition "slice-is-shaped" verdict="passed"
        condition "dependencies-delivered" verdict="passed"
        condition "no-iteration-in-flight" verdict="passed"
        condition "nothing-left-to-admit" verdict="passed"
        condition "work-is-disjoint" verdict="not-computed" \
            found="no seam manifest exists, so what two iterations would touch cannot be compared. Disjointness is half of what this capability says it judges, and half of what it judges is currently not computed at all"
        condition "the-slice-is-the-best-available" verdict="left-to-the-maintainer"
    }

    // 2 of the declared conditions were not decided by the gate. They are
    // recorded above rather than omitted: an admission that does not say what
    // it left undecided claims more than it checked.

    // Frozen at open, from the slices. Removing one from a slice now
    // does not remove it from here — it makes the two disagree, and that is
    // refused.
    claim "C1" from-slice="TS.260821.11" state="met" \
        evidence="it answers over a vocabulary this repository does not have — \
            the fixture's kinds are `cohort`, `experiment` and `receipt`, none of which this \
            engine has ever heard of, and the same code path answers"
    claim "C2" from-slice="TS.260821.11" state="met" \
        evidence="an undeclared kind or field is refused and names what is declared \
            — refused rather than answered empty, because a filter on a field that does not exist \
            matches nothing and looks exactly like a filter that matched nothing"
    claim "C3" from-slice="TS.260821.11" state="met" \
        evidence="the count is the rows of the same question — the integer alone \
            on stdout, no prose. Four commands were run twenty-five times to read four integers, \
            two with their framing stripped by sed"
    claim "C4" from-slice="TS.260821.11" state="met" \
        evidence="unreferenced by follows a declared edge, and live: \
            `praxis ask iteration --where state=closed --unreferenced-by release.binds` is the \
            `for f in an iteration record` pipeline that produced this slice"
    claim "C5" from-slice="TS.260821.11" state="met" \
        evidence="ask() takes documents and a schema and returns an Answer — there is no path in \
            the signature, so there is nowhere to write. The CLI reads with load() and prints"

    layer "doctrine"    state="evidenced" \
        evidence="surface.skill.ask-the-record, anchored and registered. 52 of 52 anchored" \
        reaches="asking the record is the first move in a repository you did not set up"
    layer "enforcement" state="evidenced" \
        evidence="an undeclared kind or field refuses; an undeclared edge refuses; a kind that is \
            only ever nested refuses rather than answering zero. Every target passes" \
        reaches="a question that would silently answer nothing cannot be asked"
    layer "docs"        state="evidenced" \
        evidence="the eight questions this session answered with grep are one command each" \
        reaches="the answer does not need sed"

    phase "design-ux" state="complete" required-by="docs" worked-by="agent:principal-engineer" \
        produced="`--count` prints the integer and nothing else, because the log says the output \
            was consumed as a scalar and the prose was actively removed"
    phase "design-system" state="complete" required-by="enforcement" worked-by="agent:principal-engineer" \
        produced="A4 forbade the obvious design — a set of named questions — and left only the \
            portable one. The same constraint that made adoption possible makes this generic"
    phase "implement" state="complete" required-by="enforcement · docs" worked-by="agent:principal-engineer" \
        produced="the ask projection, the ask command, and the nested-kind refusal"
    phase "learn" state="complete" required-by="always" worked-by="agent:principal-engineer" \
        produced="AW1 — an exclusion that produced a wrong-looking answer rather than a refusal"
    phase "teach" state="skipped" required-by="doctrine" worked-by="agent:principal-engineer" \
        produced="surface.skill.ask-the-record" \
        skipped-because="it produced doctrine for an AGENT, which is the doctrine LAYER and is recorded there. Teaching an end user the feature's why and how did not happen — and the record already serves the next agent, whose own persona judges success by NOT needing a document (TS.260821.19, ITER.260822.15/BA1)"

    finding "AW1" state="open" size="observation" \
        text="the slice excluded nested entities by name, and the exclusion produced `0` — \
            `praxis ask symptom --where state=present --count` answered zero while seven symptoms \
            existed, because they are nested in the frame" \
        note="excluding a capability is not the same as being honest about the exclusion. The \
            first is a scope decision and the second is a separate obligation, and only the first \
            was written down. A refusal now says `cannot see them` rather than answering a number \
            that reads as the truth — which is the same argument the slice already made about \
            refusing an undeclared field, applied to its own excludes"

    finding "AW2" state="open" size="observation" \
        text="`praxis ready`'s blocked section is the one output no filter expresses. Everything \
            else in this session's usage log reduces to a count or a filter; `what would refuse \
            each slice, and why` does not" \
        note="worth knowing before the next composed view is replaced by a query. A view earns \
            its length when the question has no scalar form, and that one did — it surfaced a \
            dependency cycle no amount of reading the files would have shown"
    closed-at "2026-08-22T21:18:58Z"
    outcome "continue"

    close {
        claims-settled "C1" how="met"
        claims-settled "C2" how="met"
        claims-settled "C3" how="met"
        claims-settled "C4" how="met"
        claims-settled "C5" how="met"
        all-met #true
        attested-by "human:waelrabadi"
        note "the accounting above was computed at close, not asserted. It says every claim is either met or carried by a finding that names it, and nothing about whether the work is any good"
    }

    trail {
        entry at="2026-08-22T21:18:58Z" by="agent:praxis" action="state-changed" from="open" to="closed" note="closed by `praxis close`. The accounting was computed; the conclusion is not the machine's to draw"

        entry at="2026-08-22T09:42:27Z" by="agent:praxis" action="created" \
            note="opened by the gate, on the ask of human:wael"
    }
}
