// Opened by `praxis pick-up`. Every condition below was evaluated against the
// tree as it stood at 2026-08-21T20:14:09Z, and the verdicts are recorded
// rather than summarised: a gate whose reasoning is not on the record is a gate
// you have to trust.

iteration "ITER.260821.18" {
    slug "project-the-dashboard-on-demand"
    on-slice "TS.260820.13"
    state "closed"
    opened-at "2026-08-21T20:14:09Z"
    opened-by "agent:praxis"

    approval "admission" {
        status "signed"
        signer "human:wael"
        at "2026-08-21T20:14:09Z"
        signed-by "the ask to pick up this slice"
    }

    vet "create" state="done" at="2026-08-21T20:14:09Z" {
        condition "slice-is-shaped" verdict="passed"
        condition "dependencies-delivered" verdict="passed"
        condition "no-iteration-in-flight" verdict="passed"
        condition "nothing-left-to-admit" verdict="passed"
        condition "work-is-disjoint" verdict="not-computed" \
            found="no seam manifest exists, so what two iterations would touch cannot be compared. Disjointness is half of what this capability says it judges, and half of what it judges is currently not computed at all"
        condition "the-slice-is-the-best-available" verdict="left-to-the-maintainer"
    }

    // 2 of the declared conditions were not decided by the gate. They are
    // recorded above rather than omitted: an admission that does not say what
    // it left undecided claims more than it checked.

    // Frozen at open, from the slice. Removing one from the slice now
    // does not remove it from here — it makes the two disagree, and that is
    // refused.

    claim "C1" from-slice="TS.260820.13" state="met" \
        text="composing needs no code path specific to any one read model, and no owner appears in more than one result" \
        settled-by="a test composing three results from three capabilities with no renderer change, plus an ownership check" \
        evidence="tests/dashboard.rs::c1_three_results_from_three_capabilities_compose_with_no_renderer_change \
            and ::c1_no_owner_appears_in_more_than_one_result. render_composed writes a heading per \
            part and calls the SAME renderer every other view uses — composition is ordering, not \
            interpretation"
    claim "C2" from-slice="TS.260820.13" state="met" \
        text="the dashboard is marked as of a moment, never as of a release" \
        settled-by="a golden test on the header" \
        evidence="tests/dashboard.rs::c2_the_dashboard_is_marked_as_of_a_moment_never_as_of_a_release \
            and ::c2_the_release_part_carries_the_moment_even_though_published_it_would_carry_the_version"
    claim "C3" from-slice="TS.260820.13" state="met" \
        text="asking twice across a state change gives two different answers, with no regeneration step" \
        settled-by="a test mutating state between two asks" \
        evidence="tests/dashboard.rs::c3_asking_twice_across_a_state_change_gives_two_different_answers"
    claim "C4" from-slice="TS.260820.13" state="met" \
        text="nothing is written under docs/releases/" \
        settled-by="a test asserting the published tree is byte-unchanged after composing" \
        evidence="tests/dashboard.rs::c4_composing_produces_no_file_at_all — structural, like the \
            preview: `dashboard` returns values, so there is no path in the type and nothing under \
            the archival root can be reached from here"

    vet "start" state="done" at="2026-08-21T20:12:00Z" {
        condition "the tree is clean and current" verdict="passed" found="HEAD 5e6e882"
        condition "the slice's scope is unchanged" verdict="passed" found="four claims, read line by line"
        condition "its dependencies are delivered" verdict="passed" \
            found="TS.260820.03 and TS.260820.04, both delivered. This slice composes what they \
                built and adds no view of its own — the second time in this frame a slice needed \
                no new mechanism"
    }

    frozen {
        pins "the entity-and-field schema" at="NA.260820.01/schema" at-commit="5e6e882"
        pins "read-model@v1" at="NA.260820.01/seam" at-commit="5e6e882"
        rule "a schema change while this iteration is open is a FINDING, not an edit" \
            exception="none. This iteration changed no schema, and it is the last slice in the frame"
    }

    contributes "slice-outcome-added" \
        because="project-the-dashboard-on-demand — the last of the eighteen slices this frame cut"

    layer "doctrine"    state="evidenced" evidence="skills/see-the-dashboard/SKILL.md, registered in the router" reaches="the skill teaches asking for the dashboard rather than reading a stale committed one"
    layer "enforcement" state="evidenced" evidence="167 tests pass; a capability contributing two parts is refused, and the composing function has no path in its type" reaches="the working path is gitignored, so a dashboard cannot be committed by accident"
    layer "docs"        state="skipped" \
        because="the slice declares this layer reaches `none by design — this slice's output never \
            enters docs/releases/`. Skipped with the slice's own words, the third time a slice has \
            declared a layer it does not reach"
    layer-set sealed=#true at="2026-08-21T20:12:00Z"

    phase "design-ux" state="complete" required-by="doctrine" \
        produced="the composed document names the OWNER above each part. Not decoration: the one \
            thing a reader of a composite view cannot otherwise tell is which capability is \
            answering, and that is exactly what E19 says must stay visible"
    phase "design-system" state="complete" required-by="enforcement" \
        produced="owners_are_distinct on the DOCUMENT rather than on any model, because that is \
            where the one-owner rule can be broken. And the readiness part is OMITTED when the \
            record declares no gate, rather than computed from an assumed one — its absence shows \
            as a missing owner instead of an empty section"
    phase "implement" state="complete" required-by="enforcement" \
        produced="praxis-core::dashboard, render_composed, `praxis dashboard`, and nine tests"
    phase "learn" state="complete" required-by="always" produced="three findings, one of them about the frame rather than the slice" \
        note="never skippable — it is the phase that judges the others"
    phase "teach" state="complete" required-by="doctrine" produced="skills/see-the-dashboard/SKILL.md" \
        taught="the-next-agent" \
        taught="the-next-agent"

    finding "AH1" state="open" size="observation" \
        text="the E10 falsifier is finished and did not fire. Five views, three renderers — terminal, \
            Markdown, composed — and the only conditional in any of them keys on `publishable`, a \
            property of the RESULT. No renderer contains the name of any view" \
        note="WALK.260820.01/W2 tested this on paper with one renderer and one view and could not \
            distinguish `no branch needed` from `nothing else tried yet`. It has now been tried with \
            everything this frame has, including a composite document, and the read-side split held"

    finding "AH2" state="open" size="observation" \
        text="the same read model — the-published-set-for-a-release — appears in this frame with two \
            lifetimes. Composed on demand it carries the moment; published into a release directory \
            it carries the version and no clock" \
        note="that one result can do both without a branch is the strongest single piece of evidence \
            that read-model@v1 is a seam rather than a formatting convention. It was not designed \
            for this — the lifetime split came from Y2, three iterations after the type existed"

    finding "AH3" state="open" size="frame-sized" \
        text="all eighteen slices are delivered. The fitness function `a-slice-took-more-than-one- \
            iteration` fired once, at ITER.260821.03, and was resolved by settling a residue in a \
            later iteration rather than by re-opening the slice" \
        obliges="NA.260820.01 — record against the fitness functions what actually happened, and \
            decide whether `a-finding-travelled-the-whole-loop` has fired at all" \
        note="the other falsifier is the interesting one. Findings have been raised, carried and \
            settled — S1, X1, U3, AG3 — but none has been opened by learning, triaged, addressed by \
            a slice AND closed by a release that shipped. The loop is drawn and has never completed \
            a full circuit, which is exactly what that fitness function was written to notice"
    closed-at "2026-08-21T20:17:09Z"
    outcome "continue"

    close {
        claims-settled "C1" how="met"
        claims-settled "C2" how="met"
        claims-settled "C3" how="met"
        claims-settled "C4" how="met"
        all-met #true
        note "the accounting above was computed at close, not asserted. It says every claim is either met or carried by a finding that names it, and nothing about whether the work is any good"
    }

    trail {
        entry at="2026-08-21T20:17:09Z" by="agent:praxis" action="state-changed" from="open" to="closed" note="closed by `praxis close`. The accounting was computed; the conclusion is not the machine's to draw"

        entry at="2026-08-21T20:14:09Z" by="agent:praxis" action="created" \
            note="opened by the gate, on the ask of human:wael"
    }
}
